# Mock Polling Application Configuration
# Copy this file to .env.local if you need to add custom environment variables

# This application uses mock data for development and testing
# No external database or authentication service configuration is required

# For production deployment, you may want to add:
# DATABASE_URL=your-database-connection-string
# AUTH_SECRET=your-authentication-secret
# API_BASE_URL=your-api-base-url

# Note: Any variables prefixed with NEXT_PUBLIC_ will be exposed to the browser
# Keep sensitive credentials server-side only