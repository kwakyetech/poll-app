# Supabase Configuration
# Copy this file to .env.local and replace with your actual Supabase credentials

# Your Supabase project URL (found in your Supabase dashboard)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key (found in your Supabase dashboard under Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Note: These are public environment variables (prefixed with NEXT_PUBLIC_)
# They will be exposed to the browser, which is safe for the anon key
# Never put your service role key here - it should only be used server-side